# Persist Grafana's SQLite DB so UI-created dashboards/users survive pod restarts
persistence:
  enabled: true
  size: 10Gi
  accessModes: ["ReadWriteOnce"]
  storageClassName: ebs-sc 

tempo:
  storage:
    trace:
      backend: s3 # Needs to fill out the rest later 
