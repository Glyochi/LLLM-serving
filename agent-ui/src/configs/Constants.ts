const global_keys = {
  maxTokens: "maxTokens",
  temperature: "temperature",
  seed: "seed",
  topP: "topP",
};

const default_inference_params = {
  maxTokens: 1000,
  temperature: 0.2,
  seed: 809357414,
  topP: 0.2,
};

export { global_keys, default_inference_params };
